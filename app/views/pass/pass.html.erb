<!DOCTYPE html>
<html>
  <head>
    <title></title>
  </head>
  <body>

    <!-- Go! -->
    <div class="container" style="margin-top:40px;">
      <a href="/pass/write" class="btn btn-info" role="button">합격 포트폴리오 작성하기</a>
    </div>

    <!-- Search -->
    <div class="container" style="margin-top:30px; margin-bottom:30px">
      <form class="navbar-form navbar-right" method="get" action="/pass">
        <div class="form-group">
          <input type="text" name="query" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Search</button>
      </form>
    </div>

    <!-- Pass PF -->
    <div class="container">
      <% if @search_result==1 %>
        <p><%=@passes.length%>개의 결과...</p>
      <% end %>

      <% @passes.each do |p| %>
      <div class="container" style="padding:30px; border-top:1px solid #cccccc">
        <div class="media">
          <div class="media-left">
            <img src="http://www.freeiconspng.com/uploads/flat-face-icon-23.png" class="media-object" style="width:60px">
          </div>
          <div class="media-body">
            <p class="media-heading"><%=p.user.name%></p>
            <h4><%= p.title %></h4>
            <p style="font-size:0.8em; color:gray"><%= "#{time_ago_in_words(p.created_at)} 전" %></p>
          </div>
          <div class="media-right">
            <a href="/pass/<%=p.id%>" class="btn btn-info" role="button">이동</a>
          </div>
        </div>
      </div>
      <% end %>
    </div>

  </body>
</html>

<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <div class="container">
      <!-- Search Result -->
      <% if @passes.present? %>
        <p>개의 결과...</p>
        <!-- OK -->
        <% @passes.each do |p| %>
        <div class="container" style="padding:30px; border-top:1px solid #cccccc">
          <div class="media">
            <div class="media-left">
              <img src="<%= asset_path @user %>" class="media-object" style="width:60px">
            </div>
            <div class="media-body">
              <h4 class="media-heading">User Name</h4>
              <p><%= p.content.html_safe %></p>
            </div>
            <div class="media-right">
              <a href="/home/view_pass/<%=p.id%>" class="btn btn-info" role="button">이동</a>
            </div>
          </div>
        </div>
        <% end %>

      <% else %>
        <!-- NO -->
        <p>There are no posts containing the term(s) <%= params[:query] %>.</p>
      <% end %>
    </div>

  </body>
</html>
